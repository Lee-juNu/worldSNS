<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.worlds.user.UserMapper">

	<select id= "tempSelectUser"  resultType = "com.team.worlds.user.User">
		select * from userTbl where user_id = 'yorunohosi'
	</select>

	<select id= "getMemberByID"  resultType = "com.team.worlds.user.User">
		select * from userTbl where user_id = #{user_ID}
	</select>

</mapper>